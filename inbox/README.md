# Inbox

새로 들어온 파일의 단기 드롭존이다.

- 생성 직후 결과만 둔다.
- 가능하면 `YYYY-MM-DD/provider/task/` 구조를 사용한다.
- 같은 작업 세션 안에서 분류한다.
- 미승인 후보·프로브·테스트는 `workbench/`로 이동한다.
- 승인된 자산만 `concept/`, `sheets/`, `sprites/`, `maps/`, `magazine/`, `video/`로 승격한다.
- `landing/`과 `2026/`에는 배포 목적 복제본만 둔다.

이 폴더를 장기 보관소로 사용하지 않는다.
